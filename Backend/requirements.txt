# .env loader
python-dotenv>=1.1.0

# RSS parsing
feedparser>=6.0.11

# Zero-shot classification
transformers>=4.50.0
torch>=2.0.0                    

# Kafka I/O
confluent-kafka>=2.0.0

# PostgreSQL client
psycopg2-binary>=2.9.0

# spaCy & your patterns
spacy>=3.8.0
# (download models via `python -m spacy download en_core_web_sm`)

# Geocoding
geopy>=2.0.0
# caching layers for geocoder
diskcache>=5.0.0
cachetools>=5.0.0

# HTML→text converter
beautifulsoup4>=4.13.0
lxml>=5.0.0